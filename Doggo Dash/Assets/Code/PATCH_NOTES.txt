Phase 4A Fix Pack Notes

1) Progress reset:
- ProgressResetServiceBehaviour no longer calls PlayerPrefs.DeleteAll().
- IMPORTANT: set 'progressKeys' in inspector to the exact key(s) your PlayerPrefsProgressSaveGateway uses.

2) RemoteConfig overrides clearing:
- If you want to clear all QA overrides deterministically, store overridden keys list under:
  rc.__keys  (newline-separated). Then reset can delete rc.<key> entries.

3) Cloud status in debug report:
- Optional: add CloudSaveStatusAdapterBehaviour next to CloudSaveServiceBehaviour and assign it in CopyDebugReportButtonBehaviour.
- CloudSaveServiceBehaviour should expose these public getters (add if missing):
    ProviderNameSafe, SlotKeySafe, LastStatusSafe, LastSyncUnixUtcSafe

4) Crash dedupe:
- CrashReportingServiceBehaviour now dedupes repeated Unity log errors/exceptions within ~2 seconds.

5) Performance hitch metric:
- RunPerformanceMonitorBehaviour now reports max frame time in ms: max_dt_ms.
